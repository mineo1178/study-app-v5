import type { TourRecommendation as Recommendation } from '../../../game/tour/recommendation';
export function TourRecommendation({ recommendations = [] }: { recommendations?: Recommendation[] }) { return recommendations.slice(0, 2).length ? <section className="rounded-xl bg-amber-50 p-4"><b>おすすめ</b>{recommendations.slice(0, 2).map((item) => <p key={`${item.type}-${item.targetId}`} className="mt-2 text-sm">{item.type === 'song' ? '曲を変えると' : item.type === 'leader' ? 'Leaderを変えると' : item.type === 'formation' ? '編成を変えると' : '育成すると'} {item.audienceGain > 0 ? `予想 +${item.audienceGain}人` : 'もっと近づけるよ'}{item.willSoldOut ? '・SOLD OUTできそう！' : item.willClear ? '・CLEARできそう！' : ''}</p>)}</section> : null; }
