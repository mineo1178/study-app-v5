import type { Performance } from '../../../game/types';
import type { TourStopReward } from '../../../game/tour/types';

export function TourResult({ result, remainingToClear, firstClearReward, nextStopLabel }: { result: Performance; remainingToClear?: number; firstClearReward?: TourStopReward | null; nextStopLabel?: string }) { const status = result.isSoldOut ? 'SOLD OUT!' : result.isClear ? 'CLEAR!' : remainingToClear !== undefined ? `あと${remainingToClear.toLocaleString()}人でCLEAR！` : 'もう一度チャレンジ！'; return <section className="rounded-2xl border border-violet-200 bg-white p-5 text-center shadow-sm"><p className="text-sm font-bold">{result.tourStopId}</p><p className="mt-2 text-3xl font-black">{result.audience.toLocaleString()} / {result.capacity.toLocaleString()}</p><p className="mt-2 text-xl font-black text-violet-700">{status}</p><p className="mt-3 text-sm">ライブのファン +{result.fanGain.toLocaleString()}</p>{firstClearReward && <div className="mt-4 rounded-xl bg-amber-50 p-3 text-left"><b>初回CLEARボーナス</b><p className="text-sm">ファン +{firstClearReward.fans} 活動P +{firstClearReward.activityPoints}</p></div>}{nextStopLabel && <p className="mt-4 font-bold text-violet-800">{nextStopLabel}</p>}</section>; }
