export function TourEntryCard({ status, missing = [], onOpen }: { status: 'locked' | 'open' | 'complete'; missing?: string[]; onOpen?: () => void }) { return <section className="rounded-2xl border border-violet-200 bg-white p-5"><h2 className="font-black">全国ツアー {status === 'open' ? 'OPEN!' : status === 'complete' ? '前半 COMPLETE' : '🔒'}</h2>{status === 'locked' ? <><p className="mt-2 text-sm">あと条件{missing.length}つ</p>{missing.map((item) => <p key={item} className="text-sm">・{item}</p>)}</> : <><p className="mt-2 text-sm">全国3都市ライブへ！</p>{onOpen && <button type="button" onClick={onOpen} className="mt-3 rounded-xl bg-violet-600 px-4 py-2 font-bold text-white">ツアーを見る</button>}</>}</section>; }
