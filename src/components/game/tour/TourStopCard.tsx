import type { TourStopDefinition, TourStopStatus } from '../../../game/tour/types';
export function TourStopCard({ stop, status, affinityHint, latestAudience, onSelect }: { stop: TourStopDefinition; status: TourStopStatus; affinityHint: string; latestAudience?: number; onSelect?: () => void }) { const label = status === 'sold-out' ? 'SOLD OUT' : status.toUpperCase(); return <button type="button" onClick={onSelect} disabled={status === 'locked'} className="w-full rounded-2xl border border-slate-200 bg-white p-4 text-left shadow-sm disabled:cursor-not-allowed disabled:opacity-50"><b>{stop.displayName}</b><p className="text-sm">{stop.city}・{stop.capacity.toLocaleString()}席</p><p className="mt-2 text-sm text-violet-700">{affinityHint}</p>{latestAudience !== undefined && <p className="mt-2 text-sm">{latestAudience.toLocaleString()} / {stop.capacity.toLocaleString()}</p>}<p className="mt-2 font-black" aria-label={`状態: ${label}`}>{label}</p></button>; }
